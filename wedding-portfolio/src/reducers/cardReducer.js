const initialState = {
    card: [],
    isDeleting: false,
    isEditing: false,
    isCreating: false,
    error: ""
  };

  export const cardReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
                return state;
            }
          };